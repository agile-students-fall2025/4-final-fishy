// API functions will go here

// Centralized Trip API (stubbed for now)

export async function saveTrip(trip) {
    // TODO: replace with fetch/axios to your backend
    await new Promise((r) => setTimeout(r, 250));
    return trip;
  }
  